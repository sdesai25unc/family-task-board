# Screenshots

Drop the images referenced by the main README here, using these exact filenames:

- `name-picker.png` — the four colorful name buttons (first load)
- `my-tasks.png` — the My Tasks tab with task cards
- `create-task.png` — the create-task form with assignees

Capture on Windows with **Win + Shift + S**, save with the names above, then
`git add docs/screenshots && git commit -m "Add screenshots" && git push`.
